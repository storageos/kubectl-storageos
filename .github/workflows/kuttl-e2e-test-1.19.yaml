# This file was auto-generated by hack/generate-tests.sh
name: kuttl e2e test 1.19

on: [push]

jobs:
  test:
    name: kuttl e2e test 1.19
    runs-on: ubuntu-18.04
    env:
      KUTTL: /usr/local/bin/kubectl-kuttl
      KUBECTL_STORAGEOS: /usr/local/bin/kubectl-storageos
    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ github.token }}
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.16'
      - name: Install kuttl
        run: |
          sudo curl -Lo $KUTTL https://github.com/kudobuilder/kuttl/releases/download/v0.11.1/kubectl-kuttl_0.11.1_linux_x86_64
          sudo chmod +x $KUTTL
      - name: Install kubectl-storageos
        run: |
          make _build
          sudo cp bin/kubectl-storageos $KUBECTL_STORAGEOS
      - name: Run kuttl installer 1.19
        run: kubectl-kuttl test --config e2e/kuttl/kubectl-storageos-installer-1.19.yaml
      - name: Run kuttl upgrade 1.19
        run: kubectl-kuttl test --config e2e/kuttl/kubectl-storageos-upgrade-1.19.yaml

      - uses: actions/upload-artifact@v3
        if: ${{ always() }} 
        with:
          name: kind-logs
          path: kind-logs-*
