apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-1
  namespace: default
spec:
  storageClassName: storageos
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
