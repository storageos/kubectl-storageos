apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl storageos install-portal --portal-username=storageos-portal --portal-password=storageos-portal --portal-api-url=www.foo.com --tenant-id=storageos
