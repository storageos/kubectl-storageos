apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl storageos install-portal --portal-client-id=storageos-portal --portal-secret=storageos-portal --portal-api-url=www.foo.com --tenant-id=storageos
